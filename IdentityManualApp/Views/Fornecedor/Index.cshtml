@model IEnumerable<Fornecedor>
@{
    ViewData["Title"] = "Fornecedores";
}

<h1>Fornecedores</h1>

<p><a asp-action="Create" class="btn">+ Novo fornecedor</a></p>

<table>
    <thead>
        <tr>
            <th>Nome</th>
            <th>CNPJ</th>
            <th>Telefone</th>
            <th>E-mail</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Nome</td>
            <td>@item.Cnpj</td>
            <td>@item.Telefone</td>
            <td>@item.Email</td>
            <td class="actions">
                <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                <a asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
            </td>
        </tr>
    }
    </tbody>
</table>
